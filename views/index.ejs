<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ฟอร์มสอบถามข้อมูลสุขภาพ Microleasing <%=clientIp%>
  </title>
  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" />
  <style>
    body {
      background: linear-gradient(135deg, #e0f7fa, #e1bee7);
      font-family: "Sarabun", sans-serif;
    }

    .form-card {
      background: #ffffffdd;
      border-radius: 1rem;
      box-shadow: 0 0 25px rgba(0, 0, 0, 0.1);
      padding: 2rem;
    }

    h1 {
      font-weight: 700;
      color: #6a1b9a;
    }

    .btn-primary {
      background-color: #8e24aa;
      border-color: #8e24aa;
    }

    .btn-primary:hover {
      background-color: #6a1b9a;
      border-color: #6a1b9a;
    }
  </style>
</head>

<body>
  <div id="cip" hidden>
    <%=clientIp%>
  </div>
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-8 col-md-10">
        <div class="form-card">
          <h1 class="mb-4 text-center">แบบฟอร์มสอบถามข้อมูลสุขภาพ เพื่อทำประกันสุขภาพพนักงานแบบกลุ่มของบริษัท Micro
            และในเครือ</h1>
          <form id="healthForm">
            <div class="row mb-3">
              <div class="mb-3">
                <label class="form-label">รหัสพนักงาน</label>
                <input type="text" class="form-control" id="personCde">
              </div>
              <div class="mb-3">
                <label class="form-label">E-Mail</label>
                <input type="text" class="form-control" id="email">
              </div>
              <div class="col-md-6">
                <label class="form-label">ชื่อ-นามสกุล</label>
                <input type="text" class="form-control" id="fullname">
              </div>
              <div class="col-md-6">
                <label class="form-label">อายุ</label>
                <input type="number" class="form-control" id="age">
              </div>
            </div>
            <div class="mb-3">
              <label class="form-label">เพศ</label>
              <select class="form-select" id="gender">
                <option value="">-- เลือกเพศ --</option>
                <option value="ชาย">ชาย</option>
                <option value="หญิง">หญิง</option>
                <option value="อื่นๆ">อื่นๆ</option>
              </select>
            </div>
            <div class="mb-3">
              <label class="form-label">เบอร์ติดต่อ</label>
              <input type="tel" class="form-control" id="phone">
            </div>
            <div class="mb-3">
              <label class="form-label">โรคประจำตัว</label>
              <textarea class="form-control" id="disease"></textarea>
            </div>
            <div class="mb-3">
              <label class="form-label">อาการปัจจุบัน</label>
              <textarea class="form-control" id="symptom"></textarea>
            </div>
            <div class="mb-3">
              <label class="form-label">แพ้ยา/อาหาร</label>
              <input type="text" class="form-control" id="allergy">
            </div>
            <button type="button" id="btn_submit" class="btn btn-primary px-4">ส่งข้อมูล</button>
          </form>

        </div>
      </div>
    </div>
  </div>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  <script>

    $(document).ready(function () {

      const logs = {
        ipaddr1: $('#cip').text().trim()
      };

      $.ajax({
        url: 'https://apidev01.microleasingplc.com:8001/loginLog',
        type: 'POST',
        contentType: 'application/json',
        data: JSON.stringify(logs),  // ✅ ใช้ logs
        success: function (res) {
          console.log('✅ ส่งข้อมูลสำเร็จ:', res);
        },
        error: function (err) {
          console.error('❌ ส่งข้อมูลไม่สำเร็จ:', err);
        }
      });
    })

    $('#btn_submit').on('click', function () {
      const formData = {
        personCde: $('#personCde').val(),
        email: $('#email').val(),
        fullname: $('#fullname').val(),
        age: $('#age').val(),
        gender: $('#gender').val(),
        phone: $('#phone').val(),
        disease: $('#disease').val(),
        symptom: $('#symptom').val(),
        allergy: $('#allergy').val()
      };

      console.log('📦 Data ready to send:', formData);

      // ส่งไป API ด้วย jQuery AJAX
      $.ajax({
        url: 'https://apidev01.microleasingplc.com:8001/submit',        // หรือ URL API ของคุณ
        type: 'POST',
        contentType: 'application/json',
        data: JSON.stringify(formData),
        success: function (res) {
          console.log('✅ ส่งข้อมูลสำเร็จ:', res);
          alert('บันทึกข้อมูลเรียบร้อย');

          $('#healthForm')[0].reset();
        },
        error: function (err) {
          console.error('❌ ส่งข้อมูลไม่สำเร็จ:', err);
          alert('เกิดข้อผิดพลาด');
        }
      });
    });
  </script>
</body>

</html>